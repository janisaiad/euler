# matrices de projections
# hormis l'identité, elles sont de rang 1 ou 2
# quitte à virer par permutations etc (prendre en compte dans le calcul) on dit que C3 dans vect(C1,C2)
# on considère aussi L3 idem etc..
# trace = rang d'ailleurs -> pour les matrices de rang 1 etc .. ça va être plus facile
# on peut écrire les conditions

# version naive -> tout calculer en distinguant selon la somme par exemple ou jsp quoi un truc comme ça


# si on diagonalise -> on a M=pJrp-1 avec M entière -> conditions sur p pour que M soit entière
# ensuite on détermine avec les nombres premiers entre eux etc .. pour les matrices inversibles (conditions aussi d'inégalités)
